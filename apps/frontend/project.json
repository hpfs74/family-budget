{
  "name": "@budget-app/frontend",
  "root": "apps/frontend",
  "sourceRoot": "apps/frontend/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/dist"],
      "defaultConfiguration": "production",
      "options": {
        "command": "cd apps/frontend && npx vite build",
        "cwd": "{workspaceRoot}"
      },
      "configurations": {
        "development": {
          "command": "cd apps/frontend && npx vite build --mode development"
        },
        "production": {
          "command": "cd apps/frontend && npx vite build --mode production"
        }
      }
    },
    "serve": {
      "executor": "nx:run-commands",
      "defaultConfiguration": "development",
      "options": {
        "command": "cd apps/frontend && npx vite dev --host localhost --port 4200",
        "cwd": "{workspaceRoot}"
      },
      "configurations": {
        "development": {
          "command": "cd apps/frontend && npx vite dev --host localhost --port 4200"
        },
        "production": {
          "command": "cd apps/frontend && npx vite preview --host localhost --port 4200"
        }
      }
    },
    "preview": {
      "executor": "nx:run-commands",
      "defaultConfiguration": "development",
      "options": {
        "command": "cd apps/frontend && npx vite preview --host localhost --port 4200",
        "cwd": "{workspaceRoot}"
      },
      "configurations": {
        "development": {
          "command": "cd apps/frontend && npx vite preview --host localhost --port 4200"
        },
        "production": {
          "command": "cd apps/frontend && npx vite preview --host localhost --port 4200"
        }
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/frontend/jest.config.ts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/frontend/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}